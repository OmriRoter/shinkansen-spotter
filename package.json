{
  "name": "shinkansen-spotter",
  "version": "1.0.0",
  "description": "Locate the nearest Japanese Shinkansen station and show when the next fast train passes through without stopping — with live data via Bright Data.",
  "main": "server/index.js",
  "scripts": {
    "start": "node --env-file=.env server/index.js",
    "start:no-env": "node server/index.js",
    "test": "node test/engine.test.js"
  },
  "engines": { "node": ">=18" },
  "license": "MIT",
  "private": true
}
